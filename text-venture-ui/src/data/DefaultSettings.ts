import { TextSettings } from "../model/TextSettings";

export const DefaultSettings: TextSettings = {
  textMode: "serif",
  lightMode: "dark",
  deviceMode: "mobile",
  consoleMode: "off",
  inventoryMode: "off",
  textSize: "medium",
  actionMode: "on",
};
